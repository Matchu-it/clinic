<?php
/**
 * runtime.php — XAMPP Local Configuration
 *
 * Edit these values to match your XAMPP MySQL setup.
 * Default XAMPP credentials: root / (empty password)
 */

define('DB_HOST',   'localhost');
define('DB_PORT',   3306);
define('DB_NAME',   'clinic_db');
define('DB_USER',   'root');
define('DB_PASS',   '');        // Change this if you set a MySQL root password in XAMPP
define('DB_SOCKET', '');        // Leave empty for XAMPP (uses TCP, not Unix socket)
